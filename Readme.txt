# Have everything on the same folder including the xlsx file, run the requirements in order to install the required libraries, then run the command below,
# For Tabular
type : python main.py --path validate.xlsx 

#For DQN
type : python main.py --path validate.xlsx --model DQN.pth

